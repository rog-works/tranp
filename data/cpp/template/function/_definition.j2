{%- set _parameters = parameters | select('!=', 'cls') | select('!=', 'self') -%}
{%- set is_const = decorator_query(decorators).contains('Embed.pure') -%}
{{- return_type }} {{ symbol }}({% include('function/_definition_params.j2') %}){% if is_const %} const{% endif %}{% if is_override %} override{% endif %}
