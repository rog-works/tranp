{%- set infer = ('const ' if is_const else '') + 'auto' + ('' if is_addr_p else '&') -%}
for ({{ infer }} {% if symbols | length > 1 %}[{{ symbols | join(', ') }}]{% else %}{{ symbols[0] }}{% endif %} : {{ iterates }}) {
{%- filter indent('\t') %}
{%- for statement in statements %}
{{ statement }}
{%- endfor %}
{%- endfilter %}
}
