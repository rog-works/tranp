{%- if path not in ['classmethod', 'abstractmethod', '__alias__'] %}
{{- path -}}(
	{%- for argument in arguments -%}
		{%- if loop.index0 > 0 %}, {% endif -%}
		{{- argument -}}
	{%- endfor -%}
)
{%- endif %}
