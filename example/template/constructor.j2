{%- if comment %}{{ comment }}{% else %}/** Constructor */{% endif %}
{{- decorators | select('!=', '') | join('\n') }}
{{ access }}: {{ class_symbol }}({{ parameters | select('!=', 'self') | join(', ') }}){% include 'constructor_initializer.j2' %} {
{%- filter indent('\t') %}
{%- for statement in statements %}
{{ statement }}
{%- endfor %}
{%- endfilter %}
}
