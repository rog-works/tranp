{%- if expression and return_type != 'void' -%}
[{{ binds | join(', ') }}]({{ params | join(', ') }}){% if binds %} mutable{% endif %} -> {{ return_type }} { return {{ expression }}; }
{%- elif expression and return_type == 'void' -%}
[{{ binds | join(', ') }}]({{ params | join(', ') }}){% if binds %} mutable{% endif %} -> {{ return_type }} { {{ expression }}; }
{%- else -%}
[{{ binds | join(', ') }}]({{ params | join(', ') }}){% if binds %} mutable{% endif %} -> {{ return_type }} {}
{%- endif -%}
